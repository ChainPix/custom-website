# WS-C — Per-Tool Feature Packs (50 tools x 12 items = 600)

The standard pack T1–T12 applied to every tool. Mark items `[x]` with a summary,
`[na]` with a reason when a pack item genuinely does not apply (counts as done).
Generated from lib/tools.ts registry order — do not reorder.

## /json-formatter
- [ ] C-json-formatter-T1 — unit spec for core logic (extract pure module if needed)
- [ ] C-json-formatter-T2 — e2e happy-path spec beyond the smoke test
- [ ] C-json-formatter-T3 — adopt ToolShell + CopyButton/use-copy (kill hand-rolled copy/layout)
- [ ] C-json-formatter-T4 — "Load example" sample-input button with a realistic fixture
- [ ] C-json-formatter-T5 — file import: drag-drop + picker with size guards (where input is text/file)
- [ ] C-json-formatter-T6 — export/download output with correct filename + MIME
- [ ] C-json-formatter-T7 — shareable state URL (lz-string hash), like json-formatter
- [ ] C-json-formatter-T8 — keyboard shortcuts (Ctrl+Enter run, Ctrl+K clear) + visible hints
- [ ] C-json-formatter-T9 — a11y pass: labels, focus order, aria-live status, axe clean
- [ ] C-json-formatter-T10 — SEO parity with gold standard: metadata + JSON-LD set (unique ids) + FAQ
- [ ] C-json-formatter-T11 — perf pass: worker offload for big inputs, debounce, memo sweep
- [ ] C-json-formatter-T12 — error UX: precise line/col messages, input highlighting, recovery hint

## /json-yaml
- [ ] C-json-yaml-T1 — unit spec for core logic (extract pure module if needed)
- [ ] C-json-yaml-T2 — e2e happy-path spec beyond the smoke test
- [ ] C-json-yaml-T3 — adopt ToolShell + CopyButton/use-copy (kill hand-rolled copy/layout)
- [ ] C-json-yaml-T4 — "Load example" sample-input button with a realistic fixture
- [ ] C-json-yaml-T5 — file import: drag-drop + picker with size guards (where input is text/file)
- [ ] C-json-yaml-T6 — export/download output with correct filename + MIME
- [ ] C-json-yaml-T7 — shareable state URL (lz-string hash), like json-formatter
- [ ] C-json-yaml-T8 — keyboard shortcuts (Ctrl+Enter run, Ctrl+K clear) + visible hints
- [ ] C-json-yaml-T9 — a11y pass: labels, focus order, aria-live status, axe clean
- [ ] C-json-yaml-T10 — SEO parity with gold standard: metadata + JSON-LD set (unique ids) + FAQ
- [ ] C-json-yaml-T11 — perf pass: worker offload for big inputs, debounce, memo sweep
- [ ] C-json-yaml-T12 — error UX: precise line/col messages, input highlighting, recovery hint

## /toml-yaml
- [ ] C-toml-yaml-T1 — unit spec for core logic (extract pure module if needed)
- [ ] C-toml-yaml-T2 — e2e happy-path spec beyond the smoke test
- [ ] C-toml-yaml-T3 — adopt ToolShell + CopyButton/use-copy (kill hand-rolled copy/layout)
- [ ] C-toml-yaml-T4 — "Load example" sample-input button with a realistic fixture
- [ ] C-toml-yaml-T5 — file import: drag-drop + picker with size guards (where input is text/file)
- [ ] C-toml-yaml-T6 — export/download output with correct filename + MIME
- [ ] C-toml-yaml-T7 — shareable state URL (lz-string hash), like json-formatter
- [ ] C-toml-yaml-T8 — keyboard shortcuts (Ctrl+Enter run, Ctrl+K clear) + visible hints
- [ ] C-toml-yaml-T9 — a11y pass: labels, focus order, aria-live status, axe clean
- [ ] C-toml-yaml-T10 — SEO parity with gold standard: metadata + JSON-LD set (unique ids) + FAQ
- [ ] C-toml-yaml-T11 — perf pass: worker offload for big inputs, debounce, memo sweep
- [ ] C-toml-yaml-T12 — error UX: precise line/col messages, input highlighting, recovery hint

## /csv-json
- [ ] C-csv-json-T1 — unit spec for core logic (extract pure module if needed)
- [ ] C-csv-json-T2 — e2e happy-path spec beyond the smoke test
- [ ] C-csv-json-T3 — adopt ToolShell + CopyButton/use-copy (kill hand-rolled copy/layout)
- [ ] C-csv-json-T4 — "Load example" sample-input button with a realistic fixture
- [ ] C-csv-json-T5 — file import: drag-drop + picker with size guards (where input is text/file)
- [ ] C-csv-json-T6 — export/download output with correct filename + MIME
- [ ] C-csv-json-T7 — shareable state URL (lz-string hash), like json-formatter
- [ ] C-csv-json-T8 — keyboard shortcuts (Ctrl+Enter run, Ctrl+K clear) + visible hints
- [ ] C-csv-json-T9 — a11y pass: labels, focus order, aria-live status, axe clean
- [ ] C-csv-json-T10 — SEO parity with gold standard: metadata + JSON-LD set (unique ids) + FAQ
- [ ] C-csv-json-T11 — perf pass: worker offload for big inputs, debounce, memo sweep
- [ ] C-csv-json-T12 — error UX: precise line/col messages, input highlighting, recovery hint

## /json-table
- [ ] C-json-table-T1 — unit spec for core logic (extract pure module if needed)
- [ ] C-json-table-T2 — e2e happy-path spec beyond the smoke test
- [ ] C-json-table-T3 — adopt ToolShell + CopyButton/use-copy (kill hand-rolled copy/layout)
- [ ] C-json-table-T4 — "Load example" sample-input button with a realistic fixture
- [ ] C-json-table-T5 — file import: drag-drop + picker with size guards (where input is text/file)
- [ ] C-json-table-T6 — export/download output with correct filename + MIME
- [ ] C-json-table-T7 — shareable state URL (lz-string hash), like json-formatter
- [ ] C-json-table-T8 — keyboard shortcuts (Ctrl+Enter run, Ctrl+K clear) + visible hints
- [ ] C-json-table-T9 — a11y pass: labels, focus order, aria-live status, axe clean
- [ ] C-json-table-T10 — SEO parity with gold standard: metadata + JSON-LD set (unique ids) + FAQ
- [ ] C-json-table-T11 — perf pass: worker offload for big inputs, debounce, memo sweep
- [ ] C-json-table-T12 — error UX: precise line/col messages, input highlighting, recovery hint

## /toml-ini-converter
- [ ] C-toml-ini-converter-T1 — unit spec for core logic (extract pure module if needed)
- [ ] C-toml-ini-converter-T2 — e2e happy-path spec beyond the smoke test
- [ ] C-toml-ini-converter-T3 — adopt ToolShell + CopyButton/use-copy (kill hand-rolled copy/layout)
- [ ] C-toml-ini-converter-T4 — "Load example" sample-input button with a realistic fixture
- [ ] C-toml-ini-converter-T5 — file import: drag-drop + picker with size guards (where input is text/file)
- [ ] C-toml-ini-converter-T6 — export/download output with correct filename + MIME
- [ ] C-toml-ini-converter-T7 — shareable state URL (lz-string hash), like json-formatter
- [ ] C-toml-ini-converter-T8 — keyboard shortcuts (Ctrl+Enter run, Ctrl+K clear) + visible hints
- [ ] C-toml-ini-converter-T9 — a11y pass: labels, focus order, aria-live status, axe clean
- [ ] C-toml-ini-converter-T10 — SEO parity with gold standard: metadata + JSON-LD set (unique ids) + FAQ
- [ ] C-toml-ini-converter-T11 — perf pass: worker offload for big inputs, debounce, memo sweep
- [ ] C-toml-ini-converter-T12 — error UX: precise line/col messages, input highlighting, recovery hint

## /markdown-html
- [ ] C-markdown-html-T1 — unit spec for core logic (extract pure module if needed)
- [ ] C-markdown-html-T2 — e2e happy-path spec beyond the smoke test
- [ ] C-markdown-html-T3 — adopt ToolShell + CopyButton/use-copy (kill hand-rolled copy/layout)
- [ ] C-markdown-html-T4 — "Load example" sample-input button with a realistic fixture
- [ ] C-markdown-html-T5 — file import: drag-drop + picker with size guards (where input is text/file)
- [ ] C-markdown-html-T6 — export/download output with correct filename + MIME
- [ ] C-markdown-html-T7 — shareable state URL (lz-string hash), like json-formatter
- [ ] C-markdown-html-T8 — keyboard shortcuts (Ctrl+Enter run, Ctrl+K clear) + visible hints
- [ ] C-markdown-html-T9 — a11y pass: labels, focus order, aria-live status, axe clean
- [ ] C-markdown-html-T10 — SEO parity with gold standard: metadata + JSON-LD set (unique ids) + FAQ
- [ ] C-markdown-html-T11 — perf pass: worker offload for big inputs, debounce, memo sweep
- [ ] C-markdown-html-T12 — error UX: precise line/col messages, input highlighting, recovery hint

## /data-uri
- [ ] C-data-uri-T1 — unit spec for core logic (extract pure module if needed)
- [ ] C-data-uri-T2 — e2e happy-path spec beyond the smoke test
- [ ] C-data-uri-T3 — adopt ToolShell + CopyButton/use-copy (kill hand-rolled copy/layout)
- [ ] C-data-uri-T4 — "Load example" sample-input button with a realistic fixture
- [ ] C-data-uri-T5 — file import: drag-drop + picker with size guards (where input is text/file)
- [ ] C-data-uri-T6 — export/download output with correct filename + MIME
- [ ] C-data-uri-T7 — shareable state URL (lz-string hash), like json-formatter
- [ ] C-data-uri-T8 — keyboard shortcuts (Ctrl+Enter run, Ctrl+K clear) + visible hints
- [ ] C-data-uri-T9 — a11y pass: labels, focus order, aria-live status, axe clean
- [ ] C-data-uri-T10 — SEO parity with gold standard: metadata + JSON-LD set (unique ids) + FAQ
- [ ] C-data-uri-T11 — perf pass: worker offload for big inputs, debounce, memo sweep
- [ ] C-data-uri-T12 — error UX: precise line/col messages, input highlighting, recovery hint

## /query-to-json
- [ ] C-query-to-json-T1 — unit spec for core logic (extract pure module if needed)
- [ ] C-query-to-json-T2 — e2e happy-path spec beyond the smoke test
- [ ] C-query-to-json-T3 — adopt ToolShell + CopyButton/use-copy (kill hand-rolled copy/layout)
- [ ] C-query-to-json-T4 — "Load example" sample-input button with a realistic fixture
- [ ] C-query-to-json-T5 — file import: drag-drop + picker with size guards (where input is text/file)
- [ ] C-query-to-json-T6 — export/download output with correct filename + MIME
- [ ] C-query-to-json-T7 — shareable state URL (lz-string hash), like json-formatter
- [ ] C-query-to-json-T8 — keyboard shortcuts (Ctrl+Enter run, Ctrl+K clear) + visible hints
- [ ] C-query-to-json-T9 — a11y pass: labels, focus order, aria-live status, axe clean
- [ ] C-query-to-json-T10 — SEO parity with gold standard: metadata + JSON-LD set (unique ids) + FAQ
- [ ] C-query-to-json-T11 — perf pass: worker offload for big inputs, debounce, memo sweep
- [ ] C-query-to-json-T12 — error UX: precise line/col messages, input highlighting, recovery hint

## /xml-formatter
- [ ] C-xml-formatter-T1 — unit spec for core logic (extract pure module if needed)
- [ ] C-xml-formatter-T2 — e2e happy-path spec beyond the smoke test
- [ ] C-xml-formatter-T3 — adopt ToolShell + CopyButton/use-copy (kill hand-rolled copy/layout)
- [ ] C-xml-formatter-T4 — "Load example" sample-input button with a realistic fixture
- [ ] C-xml-formatter-T5 — file import: drag-drop + picker with size guards (where input is text/file)
- [ ] C-xml-formatter-T6 — export/download output with correct filename + MIME
- [ ] C-xml-formatter-T7 — shareable state URL (lz-string hash), like json-formatter
- [ ] C-xml-formatter-T8 — keyboard shortcuts (Ctrl+Enter run, Ctrl+K clear) + visible hints
- [ ] C-xml-formatter-T9 — a11y pass: labels, focus order, aria-live status, axe clean
- [ ] C-xml-formatter-T10 — SEO parity with gold standard: metadata + JSON-LD set (unique ids) + FAQ
- [ ] C-xml-formatter-T11 — perf pass: worker offload for big inputs, debounce, memo sweep
- [ ] C-xml-formatter-T12 — error UX: precise line/col messages, input highlighting, recovery hint

## /curl-to-fetch
- [ ] C-curl-to-fetch-T1 — unit spec for core logic (extract pure module if needed)
- [ ] C-curl-to-fetch-T2 — e2e happy-path spec beyond the smoke test
- [ ] C-curl-to-fetch-T3 — adopt ToolShell + CopyButton/use-copy (kill hand-rolled copy/layout)
- [ ] C-curl-to-fetch-T4 — "Load example" sample-input button with a realistic fixture
- [ ] C-curl-to-fetch-T5 — file import: drag-drop + picker with size guards (where input is text/file)
- [ ] C-curl-to-fetch-T6 — export/download output with correct filename + MIME
- [ ] C-curl-to-fetch-T7 — shareable state URL (lz-string hash), like json-formatter
- [ ] C-curl-to-fetch-T8 — keyboard shortcuts (Ctrl+Enter run, Ctrl+K clear) + visible hints
- [ ] C-curl-to-fetch-T9 — a11y pass: labels, focus order, aria-live status, axe clean
- [ ] C-curl-to-fetch-T10 — SEO parity with gold standard: metadata + JSON-LD set (unique ids) + FAQ
- [ ] C-curl-to-fetch-T11 — perf pass: worker offload for big inputs, debounce, memo sweep
- [ ] C-curl-to-fetch-T12 — error UX: precise line/col messages, input highlighting, recovery hint

## /email-css-inliner
- [ ] C-email-css-inliner-T1 — unit spec for core logic (extract pure module if needed)
- [ ] C-email-css-inliner-T2 — e2e happy-path spec beyond the smoke test
- [ ] C-email-css-inliner-T3 — adopt ToolShell + CopyButton/use-copy (kill hand-rolled copy/layout)
- [ ] C-email-css-inliner-T4 — "Load example" sample-input button with a realistic fixture
- [ ] C-email-css-inliner-T5 — file import: drag-drop + picker with size guards (where input is text/file)
- [ ] C-email-css-inliner-T6 — export/download output with correct filename + MIME
- [ ] C-email-css-inliner-T7 — shareable state URL (lz-string hash), like json-formatter
- [ ] C-email-css-inliner-T8 — keyboard shortcuts (Ctrl+Enter run, Ctrl+K clear) + visible hints
- [ ] C-email-css-inliner-T9 — a11y pass: labels, focus order, aria-live status, axe clean
- [ ] C-email-css-inliner-T10 — SEO parity with gold standard: metadata + JSON-LD set (unique ids) + FAQ
- [ ] C-email-css-inliner-T11 — perf pass: worker offload for big inputs, debounce, memo sweep
- [ ] C-email-css-inliner-T12 — error UX: precise line/col messages, input highlighting, recovery hint

## /base64-encoder
- [ ] C-base64-encoder-T1 — unit spec for core logic (extract pure module if needed)
- [ ] C-base64-encoder-T2 — e2e happy-path spec beyond the smoke test
- [ ] C-base64-encoder-T3 — adopt ToolShell + CopyButton/use-copy (kill hand-rolled copy/layout)
- [ ] C-base64-encoder-T4 — "Load example" sample-input button with a realistic fixture
- [ ] C-base64-encoder-T5 — file import: drag-drop + picker with size guards (where input is text/file)
- [ ] C-base64-encoder-T6 — export/download output with correct filename + MIME
- [ ] C-base64-encoder-T7 — shareable state URL (lz-string hash), like json-formatter
- [ ] C-base64-encoder-T8 — keyboard shortcuts (Ctrl+Enter run, Ctrl+K clear) + visible hints
- [ ] C-base64-encoder-T9 — a11y pass: labels, focus order, aria-live status, axe clean
- [ ] C-base64-encoder-T10 — SEO parity with gold standard: metadata + JSON-LD set (unique ids) + FAQ
- [ ] C-base64-encoder-T11 — perf pass: worker offload for big inputs, debounce, memo sweep
- [ ] C-base64-encoder-T12 — error UX: precise line/col messages, input highlighting, recovery hint

## /url-encoder
- [ ] C-url-encoder-T1 — unit spec for core logic (extract pure module if needed)
- [ ] C-url-encoder-T2 — e2e happy-path spec beyond the smoke test
- [ ] C-url-encoder-T3 — adopt ToolShell + CopyButton/use-copy (kill hand-rolled copy/layout)
- [ ] C-url-encoder-T4 — "Load example" sample-input button with a realistic fixture
- [ ] C-url-encoder-T5 — file import: drag-drop + picker with size guards (where input is text/file)
- [ ] C-url-encoder-T6 — export/download output with correct filename + MIME
- [ ] C-url-encoder-T7 — shareable state URL (lz-string hash), like json-formatter
- [ ] C-url-encoder-T8 — keyboard shortcuts (Ctrl+Enter run, Ctrl+K clear) + visible hints
- [ ] C-url-encoder-T9 — a11y pass: labels, focus order, aria-live status, axe clean
- [ ] C-url-encoder-T10 — SEO parity with gold standard: metadata + JSON-LD set (unique ids) + FAQ
- [ ] C-url-encoder-T11 — perf pass: worker offload for big inputs, debounce, memo sweep
- [ ] C-url-encoder-T12 — error UX: precise line/col messages, input highlighting, recovery hint

## /html-entities
- [ ] C-html-entities-T1 — unit spec for core logic (extract pure module if needed)
- [ ] C-html-entities-T2 — e2e happy-path spec beyond the smoke test
- [ ] C-html-entities-T3 — adopt ToolShell + CopyButton/use-copy (kill hand-rolled copy/layout)
- [ ] C-html-entities-T4 — "Load example" sample-input button with a realistic fixture
- [ ] C-html-entities-T5 — file import: drag-drop + picker with size guards (where input is text/file)
- [ ] C-html-entities-T6 — export/download output with correct filename + MIME
- [ ] C-html-entities-T7 — shareable state URL (lz-string hash), like json-formatter
- [ ] C-html-entities-T8 — keyboard shortcuts (Ctrl+Enter run, Ctrl+K clear) + visible hints
- [ ] C-html-entities-T9 — a11y pass: labels, focus order, aria-live status, axe clean
- [ ] C-html-entities-T10 — SEO parity with gold standard: metadata + JSON-LD set (unique ids) + FAQ
- [ ] C-html-entities-T11 — perf pass: worker offload for big inputs, debounce, memo sweep
- [ ] C-html-entities-T12 — error UX: precise line/col messages, input highlighting, recovery hint

## /image-base64
- [ ] C-image-base64-T1 — unit spec for core logic (extract pure module if needed)
- [ ] C-image-base64-T2 — e2e happy-path spec beyond the smoke test
- [ ] C-image-base64-T3 — adopt ToolShell + CopyButton/use-copy (kill hand-rolled copy/layout)
- [ ] C-image-base64-T4 — "Load example" sample-input button with a realistic fixture
- [ ] C-image-base64-T5 — file import: drag-drop + picker with size guards (where input is text/file)
- [ ] C-image-base64-T6 — export/download output with correct filename + MIME
- [ ] C-image-base64-T7 — shareable state URL (lz-string hash), like json-formatter
- [ ] C-image-base64-T8 — keyboard shortcuts (Ctrl+Enter run, Ctrl+K clear) + visible hints
- [ ] C-image-base64-T9 — a11y pass: labels, focus order, aria-live status, axe clean
- [ ] C-image-base64-T10 — SEO parity with gold standard: metadata + JSON-LD set (unique ids) + FAQ
- [ ] C-image-base64-T11 — perf pass: worker offload for big inputs, debounce, memo sweep
- [ ] C-image-base64-T12 — error UX: precise line/col messages, input highlighting, recovery hint

## /hash-generator
- [ ] C-hash-generator-T1 — unit spec for core logic (extract pure module if needed)
- [ ] C-hash-generator-T2 — e2e happy-path spec beyond the smoke test
- [ ] C-hash-generator-T3 — adopt ToolShell + CopyButton/use-copy (kill hand-rolled copy/layout)
- [ ] C-hash-generator-T4 — "Load example" sample-input button with a realistic fixture
- [ ] C-hash-generator-T5 — file import: drag-drop + picker with size guards (where input is text/file)
- [ ] C-hash-generator-T6 — export/download output with correct filename + MIME
- [ ] C-hash-generator-T7 — shareable state URL (lz-string hash), like json-formatter
- [ ] C-hash-generator-T8 — keyboard shortcuts (Ctrl+Enter run, Ctrl+K clear) + visible hints
- [ ] C-hash-generator-T9 — a11y pass: labels, focus order, aria-live status, axe clean
- [ ] C-hash-generator-T10 — SEO parity with gold standard: metadata + JSON-LD set (unique ids) + FAQ
- [ ] C-hash-generator-T11 — perf pass: worker offload for big inputs, debounce, memo sweep
- [ ] C-hash-generator-T12 — error UX: precise line/col messages, input highlighting, recovery hint

## /uuid-generator
- [ ] C-uuid-generator-T1 — unit spec for core logic (extract pure module if needed)
- [ ] C-uuid-generator-T2 — e2e happy-path spec beyond the smoke test
- [ ] C-uuid-generator-T3 — adopt ToolShell + CopyButton/use-copy (kill hand-rolled copy/layout)
- [ ] C-uuid-generator-T4 — "Load example" sample-input button with a realistic fixture
- [ ] C-uuid-generator-T5 — file import: drag-drop + picker with size guards (where input is text/file)
- [ ] C-uuid-generator-T6 — export/download output with correct filename + MIME
- [ ] C-uuid-generator-T7 — shareable state URL (lz-string hash), like json-formatter
- [ ] C-uuid-generator-T8 — keyboard shortcuts (Ctrl+Enter run, Ctrl+K clear) + visible hints
- [ ] C-uuid-generator-T9 — a11y pass: labels, focus order, aria-live status, axe clean
- [ ] C-uuid-generator-T10 — SEO parity with gold standard: metadata + JSON-LD set (unique ids) + FAQ
- [ ] C-uuid-generator-T11 — perf pass: worker offload for big inputs, debounce, memo sweep
- [ ] C-uuid-generator-T12 — error UX: precise line/col messages, input highlighting, recovery hint

## /uuid-advanced
- [ ] C-uuid-advanced-T1 — unit spec for core logic (extract pure module if needed)
- [ ] C-uuid-advanced-T2 — e2e happy-path spec beyond the smoke test
- [ ] C-uuid-advanced-T3 — adopt ToolShell + CopyButton/use-copy (kill hand-rolled copy/layout)
- [ ] C-uuid-advanced-T4 — "Load example" sample-input button with a realistic fixture
- [ ] C-uuid-advanced-T5 — file import: drag-drop + picker with size guards (where input is text/file)
- [ ] C-uuid-advanced-T6 — export/download output with correct filename + MIME
- [ ] C-uuid-advanced-T7 — shareable state URL (lz-string hash), like json-formatter
- [ ] C-uuid-advanced-T8 — keyboard shortcuts (Ctrl+Enter run, Ctrl+K clear) + visible hints
- [ ] C-uuid-advanced-T9 — a11y pass: labels, focus order, aria-live status, axe clean
- [ ] C-uuid-advanced-T10 — SEO parity with gold standard: metadata + JSON-LD set (unique ids) + FAQ
- [ ] C-uuid-advanced-T11 — perf pass: worker offload for big inputs, debounce, memo sweep
- [ ] C-uuid-advanced-T12 — error UX: precise line/col messages, input highlighting, recovery hint

## /nanoid-generator
- [ ] C-nanoid-generator-T1 — unit spec for core logic (extract pure module if needed)
- [ ] C-nanoid-generator-T2 — e2e happy-path spec beyond the smoke test
- [ ] C-nanoid-generator-T3 — adopt ToolShell + CopyButton/use-copy (kill hand-rolled copy/layout)
- [ ] C-nanoid-generator-T4 — "Load example" sample-input button with a realistic fixture
- [ ] C-nanoid-generator-T5 — file import: drag-drop + picker with size guards (where input is text/file)
- [ ] C-nanoid-generator-T6 — export/download output with correct filename + MIME
- [ ] C-nanoid-generator-T7 — shareable state URL (lz-string hash), like json-formatter
- [ ] C-nanoid-generator-T8 — keyboard shortcuts (Ctrl+Enter run, Ctrl+K clear) + visible hints
- [ ] C-nanoid-generator-T9 — a11y pass: labels, focus order, aria-live status, axe clean
- [ ] C-nanoid-generator-T10 — SEO parity with gold standard: metadata + JSON-LD set (unique ids) + FAQ
- [ ] C-nanoid-generator-T11 — perf pass: worker offload for big inputs, debounce, memo sweep
- [ ] C-nanoid-generator-T12 — error UX: precise line/col messages, input highlighting, recovery hint

## /json-validator
- [ ] C-json-validator-T1 — unit spec for core logic (extract pure module if needed)
- [ ] C-json-validator-T2 — e2e happy-path spec beyond the smoke test
- [ ] C-json-validator-T3 — adopt ToolShell + CopyButton/use-copy (kill hand-rolled copy/layout)
- [ ] C-json-validator-T4 — "Load example" sample-input button with a realistic fixture
- [ ] C-json-validator-T5 — file import: drag-drop + picker with size guards (where input is text/file)
- [ ] C-json-validator-T6 — export/download output with correct filename + MIME
- [ ] C-json-validator-T7 — shareable state URL (lz-string hash), like json-formatter
- [ ] C-json-validator-T8 — keyboard shortcuts (Ctrl+Enter run, Ctrl+K clear) + visible hints
- [ ] C-json-validator-T9 — a11y pass: labels, focus order, aria-live status, axe clean
- [ ] C-json-validator-T10 — SEO parity with gold standard: metadata + JSON-LD set (unique ids) + FAQ
- [ ] C-json-validator-T11 — perf pass: worker offload for big inputs, debounce, memo sweep
- [ ] C-json-validator-T12 — error UX: precise line/col messages, input highlighting, recovery hint

## /json-diff
- [ ] C-json-diff-T1 — unit spec for core logic (extract pure module if needed)
- [ ] C-json-diff-T2 — e2e happy-path spec beyond the smoke test
- [ ] C-json-diff-T3 — adopt ToolShell + CopyButton/use-copy (kill hand-rolled copy/layout)
- [ ] C-json-diff-T4 — "Load example" sample-input button with a realistic fixture
- [ ] C-json-diff-T5 — file import: drag-drop + picker with size guards (where input is text/file)
- [ ] C-json-diff-T6 — export/download output with correct filename + MIME
- [ ] C-json-diff-T7 — shareable state URL (lz-string hash), like json-formatter
- [ ] C-json-diff-T8 — keyboard shortcuts (Ctrl+Enter run, Ctrl+K clear) + visible hints
- [ ] C-json-diff-T9 — a11y pass: labels, focus order, aria-live status, axe clean
- [ ] C-json-diff-T10 — SEO parity with gold standard: metadata + JSON-LD set (unique ids) + FAQ
- [ ] C-json-diff-T11 — perf pass: worker offload for big inputs, debounce, memo sweep
- [ ] C-json-diff-T12 — error UX: precise line/col messages, input highlighting, recovery hint

## /regex-tester
- [ ] C-regex-tester-T1 — unit spec for core logic (extract pure module if needed)
- [ ] C-regex-tester-T2 — e2e happy-path spec beyond the smoke test
- [ ] C-regex-tester-T3 — adopt ToolShell + CopyButton/use-copy (kill hand-rolled copy/layout)
- [ ] C-regex-tester-T4 — "Load example" sample-input button with a realistic fixture
- [ ] C-regex-tester-T5 — file import: drag-drop + picker with size guards (where input is text/file)
- [ ] C-regex-tester-T6 — export/download output with correct filename + MIME
- [ ] C-regex-tester-T7 — shareable state URL (lz-string hash), like json-formatter
- [ ] C-regex-tester-T8 — keyboard shortcuts (Ctrl+Enter run, Ctrl+K clear) + visible hints
- [ ] C-regex-tester-T9 — a11y pass: labels, focus order, aria-live status, axe clean
- [ ] C-regex-tester-T10 — SEO parity with gold standard: metadata + JSON-LD set (unique ids) + FAQ
- [ ] C-regex-tester-T11 — perf pass: worker offload for big inputs, debounce, memo sweep
- [ ] C-regex-tester-T12 — error UX: precise line/col messages, input highlighting, recovery hint

## /regex-extractor
- [ ] C-regex-extractor-T1 — unit spec for core logic (extract pure module if needed)
- [ ] C-regex-extractor-T2 — e2e happy-path spec beyond the smoke test
- [ ] C-regex-extractor-T3 — adopt ToolShell + CopyButton/use-copy (kill hand-rolled copy/layout)
- [ ] C-regex-extractor-T4 — "Load example" sample-input button with a realistic fixture
- [ ] C-regex-extractor-T5 — file import: drag-drop + picker with size guards (where input is text/file)
- [ ] C-regex-extractor-T6 — export/download output with correct filename + MIME
- [ ] C-regex-extractor-T7 — shareable state URL (lz-string hash), like json-formatter
- [ ] C-regex-extractor-T8 — keyboard shortcuts (Ctrl+Enter run, Ctrl+K clear) + visible hints
- [ ] C-regex-extractor-T9 — a11y pass: labels, focus order, aria-live status, axe clean
- [ ] C-regex-extractor-T10 — SEO parity with gold standard: metadata + JSON-LD set (unique ids) + FAQ
- [ ] C-regex-extractor-T11 — perf pass: worker offload for big inputs, debounce, memo sweep
- [ ] C-regex-extractor-T12 — error UX: precise line/col messages, input highlighting, recovery hint

## /resume-analyzer
- [ ] C-resume-analyzer-T1 — unit spec for core logic (extract pure module if needed)
- [ ] C-resume-analyzer-T2 — e2e happy-path spec beyond the smoke test
- [ ] C-resume-analyzer-T3 — adopt ToolShell + CopyButton/use-copy (kill hand-rolled copy/layout)
- [ ] C-resume-analyzer-T4 — "Load example" sample-input button with a realistic fixture
- [ ] C-resume-analyzer-T5 — file import: drag-drop + picker with size guards (where input is text/file)
- [ ] C-resume-analyzer-T6 — export/download output with correct filename + MIME
- [ ] C-resume-analyzer-T7 — shareable state URL (lz-string hash), like json-formatter
- [ ] C-resume-analyzer-T8 — keyboard shortcuts (Ctrl+Enter run, Ctrl+K clear) + visible hints
- [ ] C-resume-analyzer-T9 — a11y pass: labels, focus order, aria-live status, axe clean
- [ ] C-resume-analyzer-T10 — SEO parity with gold standard: metadata + JSON-LD set (unique ids) + FAQ
- [ ] C-resume-analyzer-T11 — perf pass: worker offload for big inputs, debounce, memo sweep
- [ ] C-resume-analyzer-T12 — error UX: precise line/col messages, input highlighting, recovery hint

## /cron-parser
- [ ] C-cron-parser-T1 — unit spec for core logic (extract pure module if needed)
- [ ] C-cron-parser-T2 — e2e happy-path spec beyond the smoke test
- [ ] C-cron-parser-T3 — adopt ToolShell + CopyButton/use-copy (kill hand-rolled copy/layout)
- [ ] C-cron-parser-T4 — "Load example" sample-input button with a realistic fixture
- [ ] C-cron-parser-T5 — file import: drag-drop + picker with size guards (where input is text/file)
- [ ] C-cron-parser-T6 — export/download output with correct filename + MIME
- [ ] C-cron-parser-T7 — shareable state URL (lz-string hash), like json-formatter
- [ ] C-cron-parser-T8 — keyboard shortcuts (Ctrl+Enter run, Ctrl+K clear) + visible hints
- [ ] C-cron-parser-T9 — a11y pass: labels, focus order, aria-live status, axe clean
- [ ] C-cron-parser-T10 — SEO parity with gold standard: metadata + JSON-LD set (unique ids) + FAQ
- [ ] C-cron-parser-T11 — perf pass: worker offload for big inputs, debounce, memo sweep
- [ ] C-cron-parser-T12 — error UX: precise line/col messages, input highlighting, recovery hint

## /cron-tester
- [ ] C-cron-tester-T1 — unit spec for core logic (extract pure module if needed)
- [ ] C-cron-tester-T2 — e2e happy-path spec beyond the smoke test
- [ ] C-cron-tester-T3 — adopt ToolShell + CopyButton/use-copy (kill hand-rolled copy/layout)
- [ ] C-cron-tester-T4 — "Load example" sample-input button with a realistic fixture
- [ ] C-cron-tester-T5 — file import: drag-drop + picker with size guards (where input is text/file)
- [ ] C-cron-tester-T6 — export/download output with correct filename + MIME
- [ ] C-cron-tester-T7 — shareable state URL (lz-string hash), like json-formatter
- [ ] C-cron-tester-T8 — keyboard shortcuts (Ctrl+Enter run, Ctrl+K clear) + visible hints
- [ ] C-cron-tester-T9 — a11y pass: labels, focus order, aria-live status, axe clean
- [ ] C-cron-tester-T10 — SEO parity with gold standard: metadata + JSON-LD set (unique ids) + FAQ
- [ ] C-cron-tester-T11 — perf pass: worker offload for big inputs, debounce, memo sweep
- [ ] C-cron-tester-T12 — error UX: precise line/col messages, input highlighting, recovery hint

## /code-minifier
- [ ] C-code-minifier-T1 — unit spec for core logic (extract pure module if needed)
- [ ] C-code-minifier-T2 — e2e happy-path spec beyond the smoke test
- [ ] C-code-minifier-T3 — adopt ToolShell + CopyButton/use-copy (kill hand-rolled copy/layout)
- [ ] C-code-minifier-T4 — "Load example" sample-input button with a realistic fixture
- [ ] C-code-minifier-T5 — file import: drag-drop + picker with size guards (where input is text/file)
- [ ] C-code-minifier-T6 — export/download output with correct filename + MIME
- [ ] C-code-minifier-T7 — shareable state URL (lz-string hash), like json-formatter
- [ ] C-code-minifier-T8 — keyboard shortcuts (Ctrl+Enter run, Ctrl+K clear) + visible hints
- [ ] C-code-minifier-T9 — a11y pass: labels, focus order, aria-live status, axe clean
- [ ] C-code-minifier-T10 — SEO parity with gold standard: metadata + JSON-LD set (unique ids) + FAQ
- [ ] C-code-minifier-T11 — perf pass: worker offload for big inputs, debounce, memo sweep
- [ ] C-code-minifier-T12 — error UX: precise line/col messages, input highlighting, recovery hint

## /sql-formatter
- [ ] C-sql-formatter-T1 — unit spec for core logic (extract pure module if needed)
- [ ] C-sql-formatter-T2 — e2e happy-path spec beyond the smoke test
- [ ] C-sql-formatter-T3 — adopt ToolShell + CopyButton/use-copy (kill hand-rolled copy/layout)
- [ ] C-sql-formatter-T4 — "Load example" sample-input button with a realistic fixture
- [ ] C-sql-formatter-T5 — file import: drag-drop + picker with size guards (where input is text/file)
- [ ] C-sql-formatter-T6 — export/download output with correct filename + MIME
- [ ] C-sql-formatter-T7 — shareable state URL (lz-string hash), like json-formatter
- [ ] C-sql-formatter-T8 — keyboard shortcuts (Ctrl+Enter run, Ctrl+K clear) + visible hints
- [ ] C-sql-formatter-T9 — a11y pass: labels, focus order, aria-live status, axe clean
- [ ] C-sql-formatter-T10 — SEO parity with gold standard: metadata + JSON-LD set (unique ids) + FAQ
- [ ] C-sql-formatter-T11 — perf pass: worker offload for big inputs, debounce, memo sweep
- [ ] C-sql-formatter-T12 — error UX: precise line/col messages, input highlighting, recovery hint

## /jwt-decoder
- [ ] C-jwt-decoder-T1 — unit spec for core logic (extract pure module if needed)
- [ ] C-jwt-decoder-T2 — e2e happy-path spec beyond the smoke test
- [ ] C-jwt-decoder-T3 — adopt ToolShell + CopyButton/use-copy (kill hand-rolled copy/layout)
- [ ] C-jwt-decoder-T4 — "Load example" sample-input button with a realistic fixture
- [ ] C-jwt-decoder-T5 — file import: drag-drop + picker with size guards (where input is text/file)
- [ ] C-jwt-decoder-T6 — export/download output with correct filename + MIME
- [ ] C-jwt-decoder-T7 — shareable state URL (lz-string hash), like json-formatter
- [ ] C-jwt-decoder-T8 — keyboard shortcuts (Ctrl+Enter run, Ctrl+K clear) + visible hints
- [ ] C-jwt-decoder-T9 — a11y pass: labels, focus order, aria-live status, axe clean
- [ ] C-jwt-decoder-T10 — SEO parity with gold standard: metadata + JSON-LD set (unique ids) + FAQ
- [ ] C-jwt-decoder-T11 — perf pass: worker offload for big inputs, debounce, memo sweep
- [ ] C-jwt-decoder-T12 — error UX: precise line/col messages, input highlighting, recovery hint

## /jwt-generator
- [ ] C-jwt-generator-T1 — unit spec for core logic (extract pure module if needed)
- [ ] C-jwt-generator-T2 — e2e happy-path spec beyond the smoke test
- [ ] C-jwt-generator-T3 — adopt ToolShell + CopyButton/use-copy (kill hand-rolled copy/layout)
- [ ] C-jwt-generator-T4 — "Load example" sample-input button with a realistic fixture
- [ ] C-jwt-generator-T5 — file import: drag-drop + picker with size guards (where input is text/file)
- [ ] C-jwt-generator-T6 — export/download output with correct filename + MIME
- [ ] C-jwt-generator-T7 — shareable state URL (lz-string hash), like json-formatter
- [ ] C-jwt-generator-T8 — keyboard shortcuts (Ctrl+Enter run, Ctrl+K clear) + visible hints
- [ ] C-jwt-generator-T9 — a11y pass: labels, focus order, aria-live status, axe clean
- [ ] C-jwt-generator-T10 — SEO parity with gold standard: metadata + JSON-LD set (unique ids) + FAQ
- [ ] C-jwt-generator-T11 — perf pass: worker offload for big inputs, debounce, memo sweep
- [ ] C-jwt-generator-T12 — error UX: precise line/col messages, input highlighting, recovery hint

## /css-units
- [ ] C-css-units-T1 — unit spec for core logic (extract pure module if needed)
- [ ] C-css-units-T2 — e2e happy-path spec beyond the smoke test
- [ ] C-css-units-T3 — adopt ToolShell + CopyButton/use-copy (kill hand-rolled copy/layout)
- [ ] C-css-units-T4 — "Load example" sample-input button with a realistic fixture
- [ ] C-css-units-T5 — file import: drag-drop + picker with size guards (where input is text/file)
- [ ] C-css-units-T6 — export/download output with correct filename + MIME
- [ ] C-css-units-T7 — shareable state URL (lz-string hash), like json-formatter
- [ ] C-css-units-T8 — keyboard shortcuts (Ctrl+Enter run, Ctrl+K clear) + visible hints
- [ ] C-css-units-T9 — a11y pass: labels, focus order, aria-live status, axe clean
- [ ] C-css-units-T10 — SEO parity with gold standard: metadata + JSON-LD set (unique ids) + FAQ
- [ ] C-css-units-T11 — perf pass: worker offload for big inputs, debounce, memo sweep
- [ ] C-css-units-T12 — error UX: precise line/col messages, input highlighting, recovery hint

## /chmod-calculator
- [ ] C-chmod-calculator-T1 — unit spec for core logic (extract pure module if needed)
- [ ] C-chmod-calculator-T2 — e2e happy-path spec beyond the smoke test
- [ ] C-chmod-calculator-T3 — adopt ToolShell + CopyButton/use-copy (kill hand-rolled copy/layout)
- [ ] C-chmod-calculator-T4 — "Load example" sample-input button with a realistic fixture
- [ ] C-chmod-calculator-T5 — file import: drag-drop + picker with size guards (where input is text/file)
- [ ] C-chmod-calculator-T6 — export/download output with correct filename + MIME
- [ ] C-chmod-calculator-T7 — shareable state URL (lz-string hash), like json-formatter
- [ ] C-chmod-calculator-T8 — keyboard shortcuts (Ctrl+Enter run, Ctrl+K clear) + visible hints
- [ ] C-chmod-calculator-T9 — a11y pass: labels, focus order, aria-live status, axe clean
- [ ] C-chmod-calculator-T10 — SEO parity with gold standard: metadata + JSON-LD set (unique ids) + FAQ
- [ ] C-chmod-calculator-T11 — perf pass: worker offload for big inputs, debounce, memo sweep
- [ ] C-chmod-calculator-T12 — error UX: precise line/col messages, input highlighting, recovery hint

## /cron-generator
- [ ] C-cron-generator-T1 — unit spec for core logic (extract pure module if needed)
- [ ] C-cron-generator-T2 — e2e happy-path spec beyond the smoke test
- [ ] C-cron-generator-T3 — adopt ToolShell + CopyButton/use-copy (kill hand-rolled copy/layout)
- [ ] C-cron-generator-T4 — "Load example" sample-input button with a realistic fixture
- [ ] C-cron-generator-T5 — file import: drag-drop + picker with size guards (where input is text/file)
- [ ] C-cron-generator-T6 — export/download output with correct filename + MIME
- [ ] C-cron-generator-T7 — shareable state URL (lz-string hash), like json-formatter
- [ ] C-cron-generator-T8 — keyboard shortcuts (Ctrl+Enter run, Ctrl+K clear) + visible hints
- [ ] C-cron-generator-T9 — a11y pass: labels, focus order, aria-live status, axe clean
- [ ] C-cron-generator-T10 — SEO parity with gold standard: metadata + JSON-LD set (unique ids) + FAQ
- [ ] C-cron-generator-T11 — perf pass: worker offload for big inputs, debounce, memo sweep
- [ ] C-cron-generator-T12 — error UX: precise line/col messages, input highlighting, recovery hint

## /text-case
- [ ] C-text-case-T1 — unit spec for core logic (extract pure module if needed)
- [ ] C-text-case-T2 — e2e happy-path spec beyond the smoke test
- [ ] C-text-case-T3 — adopt ToolShell + CopyButton/use-copy (kill hand-rolled copy/layout)
- [ ] C-text-case-T4 — "Load example" sample-input button with a realistic fixture
- [ ] C-text-case-T5 — file import: drag-drop + picker with size guards (where input is text/file)
- [ ] C-text-case-T6 — export/download output with correct filename + MIME
- [ ] C-text-case-T7 — shareable state URL (lz-string hash), like json-formatter
- [ ] C-text-case-T8 — keyboard shortcuts (Ctrl+Enter run, Ctrl+K clear) + visible hints
- [ ] C-text-case-T9 — a11y pass: labels, focus order, aria-live status, axe clean
- [ ] C-text-case-T10 — SEO parity with gold standard: metadata + JSON-LD set (unique ids) + FAQ
- [ ] C-text-case-T11 — perf pass: worker offload for big inputs, debounce, memo sweep
- [ ] C-text-case-T12 — error UX: precise line/col messages, input highlighting, recovery hint

## /text-search
- [ ] C-text-search-T1 — unit spec for core logic (extract pure module if needed)
- [ ] C-text-search-T2 — e2e happy-path spec beyond the smoke test
- [ ] C-text-search-T3 — adopt ToolShell + CopyButton/use-copy (kill hand-rolled copy/layout)
- [ ] C-text-search-T4 — "Load example" sample-input button with a realistic fixture
- [ ] C-text-search-T5 — file import: drag-drop + picker with size guards (where input is text/file)
- [ ] C-text-search-T6 — export/download output with correct filename + MIME
- [ ] C-text-search-T7 — shareable state URL (lz-string hash), like json-formatter
- [ ] C-text-search-T8 — keyboard shortcuts (Ctrl+Enter run, Ctrl+K clear) + visible hints
- [ ] C-text-search-T9 — a11y pass: labels, focus order, aria-live status, axe clean
- [ ] C-text-search-T10 — SEO parity with gold standard: metadata + JSON-LD set (unique ids) + FAQ
- [ ] C-text-search-T11 — perf pass: worker offload for big inputs, debounce, memo sweep
- [ ] C-text-search-T12 — error UX: precise line/col messages, input highlighting, recovery hint

## /text-deduper
- [ ] C-text-deduper-T1 — unit spec for core logic (extract pure module if needed)
- [ ] C-text-deduper-T2 — e2e happy-path spec beyond the smoke test
- [ ] C-text-deduper-T3 — adopt ToolShell + CopyButton/use-copy (kill hand-rolled copy/layout)
- [ ] C-text-deduper-T4 — "Load example" sample-input button with a realistic fixture
- [ ] C-text-deduper-T5 — file import: drag-drop + picker with size guards (where input is text/file)
- [ ] C-text-deduper-T6 — export/download output with correct filename + MIME
- [ ] C-text-deduper-T7 — shareable state URL (lz-string hash), like json-formatter
- [ ] C-text-deduper-T8 — keyboard shortcuts (Ctrl+Enter run, Ctrl+K clear) + visible hints
- [ ] C-text-deduper-T9 — a11y pass: labels, focus order, aria-live status, axe clean
- [ ] C-text-deduper-T10 — SEO parity with gold standard: metadata + JSON-LD set (unique ids) + FAQ
- [ ] C-text-deduper-T11 — perf pass: worker offload for big inputs, debounce, memo sweep
- [ ] C-text-deduper-T12 — error UX: precise line/col messages, input highlighting, recovery hint

## /markdown-preview
- [ ] C-markdown-preview-T1 — unit spec for core logic (extract pure module if needed)
- [ ] C-markdown-preview-T2 — e2e happy-path spec beyond the smoke test
- [ ] C-markdown-preview-T3 — adopt ToolShell + CopyButton/use-copy (kill hand-rolled copy/layout)
- [ ] C-markdown-preview-T4 — "Load example" sample-input button with a realistic fixture
- [ ] C-markdown-preview-T5 — file import: drag-drop + picker with size guards (where input is text/file)
- [ ] C-markdown-preview-T6 — export/download output with correct filename + MIME
- [ ] C-markdown-preview-T7 — shareable state URL (lz-string hash), like json-formatter
- [ ] C-markdown-preview-T8 — keyboard shortcuts (Ctrl+Enter run, Ctrl+K clear) + visible hints
- [ ] C-markdown-preview-T9 — a11y pass: labels, focus order, aria-live status, axe clean
- [ ] C-markdown-preview-T10 — SEO parity with gold standard: metadata + JSON-LD set (unique ids) + FAQ
- [ ] C-markdown-preview-T11 — perf pass: worker offload for big inputs, debounce, memo sweep
- [ ] C-markdown-preview-T12 — error UX: precise line/col messages, input highlighting, recovery hint

## /lorem-ipsum
- [ ] C-lorem-ipsum-T1 — unit spec for core logic (extract pure module if needed)
- [ ] C-lorem-ipsum-T2 — e2e happy-path spec beyond the smoke test
- [ ] C-lorem-ipsum-T3 — adopt ToolShell + CopyButton/use-copy (kill hand-rolled copy/layout)
- [ ] C-lorem-ipsum-T4 — "Load example" sample-input button with a realistic fixture
- [ ] C-lorem-ipsum-T5 — file import: drag-drop + picker with size guards (where input is text/file)
- [ ] C-lorem-ipsum-T6 — export/download output with correct filename + MIME
- [ ] C-lorem-ipsum-T7 — shareable state URL (lz-string hash), like json-formatter
- [ ] C-lorem-ipsum-T8 — keyboard shortcuts (Ctrl+Enter run, Ctrl+K clear) + visible hints
- [ ] C-lorem-ipsum-T9 — a11y pass: labels, focus order, aria-live status, axe clean
- [ ] C-lorem-ipsum-T10 — SEO parity with gold standard: metadata + JSON-LD set (unique ids) + FAQ
- [ ] C-lorem-ipsum-T11 — perf pass: worker offload for big inputs, debounce, memo sweep
- [ ] C-lorem-ipsum-T12 — error UX: precise line/col messages, input highlighting, recovery hint

## /number-formatter
- [ ] C-number-formatter-T1 — unit spec for core logic (extract pure module if needed)
- [ ] C-number-formatter-T2 — e2e happy-path spec beyond the smoke test
- [ ] C-number-formatter-T3 — adopt ToolShell + CopyButton/use-copy (kill hand-rolled copy/layout)
- [ ] C-number-formatter-T4 — "Load example" sample-input button with a realistic fixture
- [ ] C-number-formatter-T5 — file import: drag-drop + picker with size guards (where input is text/file)
- [ ] C-number-formatter-T6 — export/download output with correct filename + MIME
- [ ] C-number-formatter-T7 — shareable state URL (lz-string hash), like json-formatter
- [ ] C-number-formatter-T8 — keyboard shortcuts (Ctrl+Enter run, Ctrl+K clear) + visible hints
- [ ] C-number-formatter-T9 — a11y pass: labels, focus order, aria-live status, axe clean
- [ ] C-number-formatter-T10 — SEO parity with gold standard: metadata + JSON-LD set (unique ids) + FAQ
- [ ] C-number-formatter-T11 — perf pass: worker offload for big inputs, debounce, memo sweep
- [ ] C-number-formatter-T12 — error UX: precise line/col messages, input highlighting, recovery hint

## /timestamp-converter
- [ ] C-timestamp-converter-T1 — unit spec for core logic (extract pure module if needed)
- [ ] C-timestamp-converter-T2 — e2e happy-path spec beyond the smoke test
- [ ] C-timestamp-converter-T3 — adopt ToolShell + CopyButton/use-copy (kill hand-rolled copy/layout)
- [ ] C-timestamp-converter-T4 — "Load example" sample-input button with a realistic fixture
- [ ] C-timestamp-converter-T5 — file import: drag-drop + picker with size guards (where input is text/file)
- [ ] C-timestamp-converter-T6 — export/download output with correct filename + MIME
- [ ] C-timestamp-converter-T7 — shareable state URL (lz-string hash), like json-formatter
- [ ] C-timestamp-converter-T8 — keyboard shortcuts (Ctrl+Enter run, Ctrl+K clear) + visible hints
- [ ] C-timestamp-converter-T9 — a11y pass: labels, focus order, aria-live status, axe clean
- [ ] C-timestamp-converter-T10 — SEO parity with gold standard: metadata + JSON-LD set (unique ids) + FAQ
- [ ] C-timestamp-converter-T11 — perf pass: worker offload for big inputs, debounce, memo sweep
- [ ] C-timestamp-converter-T12 — error UX: precise line/col messages, input highlighting, recovery hint

## /color-converter
- [ ] C-color-converter-T1 — unit spec for core logic (extract pure module if needed)
- [ ] C-color-converter-T2 — e2e happy-path spec beyond the smoke test
- [ ] C-color-converter-T3 — adopt ToolShell + CopyButton/use-copy (kill hand-rolled copy/layout)
- [ ] C-color-converter-T4 — "Load example" sample-input button with a realistic fixture
- [ ] C-color-converter-T5 — file import: drag-drop + picker with size guards (where input is text/file)
- [ ] C-color-converter-T6 — export/download output with correct filename + MIME
- [ ] C-color-converter-T7 — shareable state URL (lz-string hash), like json-formatter
- [ ] C-color-converter-T8 — keyboard shortcuts (Ctrl+Enter run, Ctrl+K clear) + visible hints
- [ ] C-color-converter-T9 — a11y pass: labels, focus order, aria-live status, axe clean
- [ ] C-color-converter-T10 — SEO parity with gold standard: metadata + JSON-LD set (unique ids) + FAQ
- [ ] C-color-converter-T11 — perf pass: worker offload for big inputs, debounce, memo sweep
- [ ] C-color-converter-T12 — error UX: precise line/col messages, input highlighting, recovery hint

## /diff-viewer
- [ ] C-diff-viewer-T1 — unit spec for core logic (extract pure module if needed)
- [ ] C-diff-viewer-T2 — e2e happy-path spec beyond the smoke test
- [ ] C-diff-viewer-T3 — adopt ToolShell + CopyButton/use-copy (kill hand-rolled copy/layout)
- [ ] C-diff-viewer-T4 — "Load example" sample-input button with a realistic fixture
- [ ] C-diff-viewer-T5 — file import: drag-drop + picker with size guards (where input is text/file)
- [ ] C-diff-viewer-T6 — export/download output with correct filename + MIME
- [ ] C-diff-viewer-T7 — shareable state URL (lz-string hash), like json-formatter
- [ ] C-diff-viewer-T8 — keyboard shortcuts (Ctrl+Enter run, Ctrl+K clear) + visible hints
- [ ] C-diff-viewer-T9 — a11y pass: labels, focus order, aria-live status, axe clean
- [ ] C-diff-viewer-T10 — SEO parity with gold standard: metadata + JSON-LD set (unique ids) + FAQ
- [ ] C-diff-viewer-T11 — perf pass: worker offload for big inputs, debounce, memo sweep
- [ ] C-diff-viewer-T12 — error UX: precise line/col messages, input highlighting, recovery hint

## /qr-generator
- [ ] C-qr-generator-T1 — unit spec for core logic (extract pure module if needed)
- [ ] C-qr-generator-T2 — e2e happy-path spec beyond the smoke test
- [ ] C-qr-generator-T3 — adopt ToolShell + CopyButton/use-copy (kill hand-rolled copy/layout)
- [ ] C-qr-generator-T4 — "Load example" sample-input button with a realistic fixture
- [ ] C-qr-generator-T5 — file import: drag-drop + picker with size guards (where input is text/file)
- [ ] C-qr-generator-T6 — export/download output with correct filename + MIME
- [ ] C-qr-generator-T7 — shareable state URL (lz-string hash), like json-formatter
- [ ] C-qr-generator-T8 — keyboard shortcuts (Ctrl+Enter run, Ctrl+K clear) + visible hints
- [ ] C-qr-generator-T9 — a11y pass: labels, focus order, aria-live status, axe clean
- [ ] C-qr-generator-T10 — SEO parity with gold standard: metadata + JSON-LD set (unique ids) + FAQ
- [ ] C-qr-generator-T11 — perf pass: worker offload for big inputs, debounce, memo sweep
- [ ] C-qr-generator-T12 — error UX: precise line/col messages, input highlighting, recovery hint

## /password-generator
- [ ] C-password-generator-T1 — unit spec for core logic (extract pure module if needed)
- [ ] C-password-generator-T2 — e2e happy-path spec beyond the smoke test
- [ ] C-password-generator-T3 — adopt ToolShell + CopyButton/use-copy (kill hand-rolled copy/layout)
- [ ] C-password-generator-T4 — "Load example" sample-input button with a realistic fixture
- [ ] C-password-generator-T5 — file import: drag-drop + picker with size guards (where input is text/file)
- [ ] C-password-generator-T6 — export/download output with correct filename + MIME
- [ ] C-password-generator-T7 — shareable state URL (lz-string hash), like json-formatter
- [ ] C-password-generator-T8 — keyboard shortcuts (Ctrl+Enter run, Ctrl+K clear) + visible hints
- [ ] C-password-generator-T9 — a11y pass: labels, focus order, aria-live status, axe clean
- [ ] C-password-generator-T10 — SEO parity with gold standard: metadata + JSON-LD set (unique ids) + FAQ
- [ ] C-password-generator-T11 — perf pass: worker offload for big inputs, debounce, memo sweep
- [ ] C-password-generator-T12 — error UX: precise line/col messages, input highlighting, recovery hint

## /mock-data
- [ ] C-mock-data-T1 — unit spec for core logic (extract pure module if needed)
- [ ] C-mock-data-T2 — e2e happy-path spec beyond the smoke test
- [ ] C-mock-data-T3 — adopt ToolShell + CopyButton/use-copy (kill hand-rolled copy/layout)
- [ ] C-mock-data-T4 — "Load example" sample-input button with a realistic fixture
- [ ] C-mock-data-T5 — file import: drag-drop + picker with size guards (where input is text/file)
- [ ] C-mock-data-T6 — export/download output with correct filename + MIME
- [ ] C-mock-data-T7 — shareable state URL (lz-string hash), like json-formatter
- [ ] C-mock-data-T8 — keyboard shortcuts (Ctrl+Enter run, Ctrl+K clear) + visible hints
- [ ] C-mock-data-T9 — a11y pass: labels, focus order, aria-live status, axe clean
- [ ] C-mock-data-T10 — SEO parity with gold standard: metadata + JSON-LD set (unique ids) + FAQ
- [ ] C-mock-data-T11 — perf pass: worker offload for big inputs, debounce, memo sweep
- [ ] C-mock-data-T12 — error UX: precise line/col messages, input highlighting, recovery hint

## /ip-asn-lookup
- [ ] C-ip-asn-lookup-T1 — unit spec for core logic (extract pure module if needed)
- [ ] C-ip-asn-lookup-T2 — e2e happy-path spec beyond the smoke test
- [ ] C-ip-asn-lookup-T3 — adopt ToolShell + CopyButton/use-copy (kill hand-rolled copy/layout)
- [ ] C-ip-asn-lookup-T4 — "Load example" sample-input button with a realistic fixture
- [ ] C-ip-asn-lookup-T5 — file import: drag-drop + picker with size guards (where input is text/file)
- [ ] C-ip-asn-lookup-T6 — export/download output with correct filename + MIME
- [ ] C-ip-asn-lookup-T7 — shareable state URL (lz-string hash), like json-formatter
- [ ] C-ip-asn-lookup-T8 — keyboard shortcuts (Ctrl+Enter run, Ctrl+K clear) + visible hints
- [ ] C-ip-asn-lookup-T9 — a11y pass: labels, focus order, aria-live status, axe clean
- [ ] C-ip-asn-lookup-T10 — SEO parity with gold standard: metadata + JSON-LD set (unique ids) + FAQ
- [ ] C-ip-asn-lookup-T11 — perf pass: worker offload for big inputs, debounce, memo sweep
- [ ] C-ip-asn-lookup-T12 — error UX: precise line/col messages, input highlighting, recovery hint

## /url-parser
- [ ] C-url-parser-T1 — unit spec for core logic (extract pure module if needed)
- [ ] C-url-parser-T2 — e2e happy-path spec beyond the smoke test
- [ ] C-url-parser-T3 — adopt ToolShell + CopyButton/use-copy (kill hand-rolled copy/layout)
- [ ] C-url-parser-T4 — "Load example" sample-input button with a realistic fixture
- [ ] C-url-parser-T5 — file import: drag-drop + picker with size guards (where input is text/file)
- [ ] C-url-parser-T6 — export/download output with correct filename + MIME
- [ ] C-url-parser-T7 — shareable state URL (lz-string hash), like json-formatter
- [ ] C-url-parser-T8 — keyboard shortcuts (Ctrl+Enter run, Ctrl+K clear) + visible hints
- [ ] C-url-parser-T9 — a11y pass: labels, focus order, aria-live status, axe clean
- [ ] C-url-parser-T10 — SEO parity with gold standard: metadata + JSON-LD set (unique ids) + FAQ
- [ ] C-url-parser-T11 — perf pass: worker offload for big inputs, debounce, memo sweep
- [ ] C-url-parser-T12 — error UX: precise line/col messages, input highlighting, recovery hint

## /webp-converter
- [ ] C-webp-converter-T1 — unit spec for core logic (extract pure module if needed)
- [ ] C-webp-converter-T2 — e2e happy-path spec beyond the smoke test
- [ ] C-webp-converter-T3 — adopt ToolShell + CopyButton/use-copy (kill hand-rolled copy/layout)
- [ ] C-webp-converter-T4 — "Load example" sample-input button with a realistic fixture
- [ ] C-webp-converter-T5 — file import: drag-drop + picker with size guards (where input is text/file)
- [ ] C-webp-converter-T6 — export/download output with correct filename + MIME
- [ ] C-webp-converter-T7 — shareable state URL (lz-string hash), like json-formatter
- [ ] C-webp-converter-T8 — keyboard shortcuts (Ctrl+Enter run, Ctrl+K clear) + visible hints
- [ ] C-webp-converter-T9 — a11y pass: labels, focus order, aria-live status, axe clean
- [ ] C-webp-converter-T10 — SEO parity with gold standard: metadata + JSON-LD set (unique ids) + FAQ
- [ ] C-webp-converter-T11 — perf pass: worker offload for big inputs, debounce, memo sweep
- [ ] C-webp-converter-T12 — error UX: precise line/col messages, input highlighting, recovery hint

## /pdf-to-text
- [ ] C-pdf-to-text-T1 — unit spec for core logic (extract pure module if needed)
- [ ] C-pdf-to-text-T2 — e2e happy-path spec beyond the smoke test
- [ ] C-pdf-to-text-T3 — adopt ToolShell + CopyButton/use-copy (kill hand-rolled copy/layout)
- [ ] C-pdf-to-text-T4 — "Load example" sample-input button with a realistic fixture
- [ ] C-pdf-to-text-T5 — file import: drag-drop + picker with size guards (where input is text/file)
- [ ] C-pdf-to-text-T6 — export/download output with correct filename + MIME
- [ ] C-pdf-to-text-T7 — shareable state URL (lz-string hash), like json-formatter
- [ ] C-pdf-to-text-T8 — keyboard shortcuts (Ctrl+Enter run, Ctrl+K clear) + visible hints
- [ ] C-pdf-to-text-T9 — a11y pass: labels, focus order, aria-live status, axe clean
- [ ] C-pdf-to-text-T10 — SEO parity with gold standard: metadata + JSON-LD set (unique ids) + FAQ
- [ ] C-pdf-to-text-T11 — perf pass: worker offload for big inputs, debounce, memo sweep
- [ ] C-pdf-to-text-T12 — error UX: precise line/col messages, input highlighting, recovery hint
